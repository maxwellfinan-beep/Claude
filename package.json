{
  "name": "discord-duck-hunt",
  "version": "1.0.0",
  "description": "Visual Duck Hunt Discord Activity — multiplayer game with live leaderboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.18.0",
    "socket.io": "^4.7.0"
  }
}
