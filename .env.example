DISCORD_TOKEN=your-bot-token-here

# Optional — defaults shown
DUCK_CHANNEL=duck-hunt      # text channel where ducks spawn
DUCK_SPAWN_MIN=20           # min seconds between duck spawns
DUCK_SPAWN_MAX=120          # max seconds between duck spawns
DB_PATH=scores.db           # SQLite file for persistent scores
